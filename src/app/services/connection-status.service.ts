import { Injectable } from '@angular/core';

/**
 * ConnectionStatusService
 *
 * Placeholder - backend connection validation is currently disabled.
 * Authentication status is handled by the NgRx auth state (OidcService + auth reducer).
 */
@Injectable({ providedIn: 'root' })
export class ConnectionStatusService {
  constructor() {}
}
