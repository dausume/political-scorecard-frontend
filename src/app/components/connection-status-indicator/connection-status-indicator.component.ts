import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

/**
 * ConnectionStatusIndicatorComponent
 *
 * Placeholder - backend connection status indicator is currently disabled.
 * Authentication status is shown via the header bar's auth state.
 */
@Component({
  selector: 'app-connection-status-indicator',
  standalone: true,
  imports: [CommonModule],
  template: ''
})
export class ConnectionStatusIndicatorComponent {}
